# Shared pnpm store (all branches use same cache)
store-dir=../.pnpm-store

# Use strict peer dependencies
strict-peer-dependencies=false

# Shamefully hoist (for compatibility)
shamefully-hoist=true
